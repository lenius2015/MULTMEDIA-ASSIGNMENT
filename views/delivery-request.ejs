<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Request Delivery - OMUNJU SHOPPERS</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        .delivery-container {
            max-width: 800px;
            margin: 40px auto;
            padding: 0 20px;
        }

        .delivery-header {
            text-align: center;
            margin-bottom: 40px;
        }

        .delivery-title {
            font-size: 2.5rem;
            color: #333;
            margin-bottom: 10px;
        }

        .delivery-subtitle {
            color: #666;
            font-size: 1.1rem;
        }

        .order-summary {
            background: #f8f9fa;
            border-radius: 12px;
            padding: 25px;
            margin-bottom: 30px;
            border: 1px solid #e0e0e0;
        }

        .order-summary h3 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.3rem;
        }

        .order-details {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .order-detail-item {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .order-detail-item:last-child {
            border-bottom: none;
            font-weight: 600;
            color: #333;
        }

        .delivery-form {
            background: white;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            border: 1px solid #e0e0e0;
        }

        .form-section {
            margin-bottom: 30px;
        }

        .form-section h4 {
            color: #333;
            margin-bottom: 15px;
            font-size: 1.2rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-section h4 i {
            color: #667eea;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-label {
            font-weight: 500;
            margin-bottom: 8px;
            color: #333;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .form-label i {
            color: #667eea;
            font-size: 0.9rem;
        }

        .form-input, .form-textarea, .form-select {
            padding: 12px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: white;
        }

        .form-input:focus, .form-textarea:focus, .form-select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .form-textarea {
            resize: vertical;
            min-height: 80px;
        }

        .delivery-methods {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .delivery-method {
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s ease;
            background: white;
        }

        .delivery-method:hover {
            border-color: #667eea;
            transform: translateY(-2px);
        }

        .delivery-method.selected {
            border-color: #667eea;
            background: #f8f9ff;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.2);
        }

        .method-icon {
            font-size: 2rem;
            color: #667eea;
            margin-bottom: 10px;
        }

        .method-title {
            font-weight: 600;
            margin-bottom: 5px;
            color: #333;
        }

        .method-description {
            color: #666;
            font-size: 0.9rem;
        }

        .method-price {
            color: #28a745;
            font-weight: 600;
            margin-top: 5px;
        }

        .form-actions {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 30px;
        }

        .btn {
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
        }

        .btn-secondary {
            background: #f8f9fa;
            color: #333;
            border: 2px solid #e0e0e0;
        }

        .btn-secondary:hover {
            background: #e9ecef;
            border-color: #adb5bd;
        }

        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            display: none;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-error {
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }

        .loading {
            text-align: center;
            padding: 40px;
        }

        .loading i {
            font-size: 2rem;
            color: #667eea;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        @media (max-width: 768px) {
            .form-row {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .delivery-methods {
                grid-template-columns: 1fr;
            }

            .order-details {
                grid-template-columns: 1fr;
            }

            .delivery-title {
                font-size: 2rem;
            }

            .form-actions {
                flex-direction: column;
            }

            .btn {
                width: 100%;
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <%- include('./partials/header', { currentPage: 'orders' }) %>

    <div class="delivery-container">
        <!-- Delivery Header -->
        <div class="delivery-header">
            <h1 class="delivery-title">Request Delivery</h1>
            <p class="delivery-subtitle">Complete your order delivery details</p>
        </div>

        <!-- Order Summary -->
        <div class="order-summary">
            <h3><i class="fas fa-shopping-bag"></i> Order Summary</h3>
            <div class="order-details" id="orderDetails">
                <div class="loading">
                    <i class="fas fa-spinner"></i>
                    <p>Loading order details...</p>
                </div>
            </div>
        </div>

        <!-- Delivery Request Form -->
        <form class="delivery-form" id="deliveryForm">
            <div id="alertMessage" class="alert"></div>

            <!-- Delivery Address -->
            <div class="form-section">
                <h4><i class="fas fa-map-marker-alt"></i> Delivery Address</h4>
                <div class="form-group full-width">
                    <label for="deliveryAddress" class="form-label">
                        <i class="fas fa-home"></i>
                        Full Delivery Address
                    </label>
                    <textarea id="deliveryAddress" name="deliveryAddress" class="form-textarea"
                        placeholder="Enter your complete delivery address including street, building, area, and landmarks" required></textarea>
                </div>
            </div>

            <!-- Contact Information -->
            <div class="form-section">
                <h4><i class="fas fa-phone"></i> Contact Information</h4>
                <div class="form-row">
                    <div class="form-group">
                        <label for="phoneNumber" class="form-label">
                            <i class="fas fa-mobile-alt"></i>
                            Phone Number
                        </label>
                        <input type="tel" id="phoneNumber" name="phoneNumber" class="form-input"
                            placeholder="+254 XXX XXX XXX" required>
                    </div>
                    <div class="form-group">
                        <label for="preferredDate" class="form-label">
                            <i class="fas fa-calendar-alt"></i>
                            Preferred Delivery Date (Optional)
                        </label>
                        <input type="date" id="preferredDate" name="preferredDate" class="form-input"
                            min="<%= new Date(Date.now() + 24 * 60 * 60 * 1000).toISOString().split('T')[0] %>">
                    </div>
                </div>
            </div>

            <!-- Delivery Method -->
            <div class="form-section">
                <h4><i class="fas fa-truck"></i> Delivery Method</h4>
                <div class="delivery-methods">
                    <div class="delivery-method" data-method="standard">
                        <div class="method-icon">
                            <i class="fas fa-box"></i>
                        </div>
                        <div class="method-title">Standard Delivery</div>
                        <div class="method-description">3-5 business days</div>
                        <div class="method-price">Free</div>
                    </div>
                    <div class="delivery-method" data-method="express">
                        <div class="method-icon">
                            <i class="fas fa-rocket"></i>
                        </div>
                        <div class="method-title">Express Delivery</div>
                        <div class="method-description">1-2 business days</div>
                        <div class="method-price">TSH 500</div>
                    </div>
                </div>
                <input type="hidden" id="deliveryMethod" name="deliveryMethod" value="standard">
            </div>

            <!-- Special Instructions -->
            <div class="form-section">
                <h4><i class="fas fa-sticky-note"></i> Special Instructions (Optional)</h4>
                <div class="form-group full-width">
                    <label for="deliveryInstructions" class="form-label">
                        <i class="fas fa-info-circle"></i>
                        Delivery Instructions
                    </label>
                    <textarea id="deliveryInstructions" name="deliveryInstructions" class="form-textarea"
                        placeholder="Any special instructions for delivery (e.g., gate code, delivery time preferences, etc.)"></textarea>
                </div>
            </div>

            <!-- Form Actions -->
            <div class="form-actions">
                <button type="button" class="btn btn-secondary" onclick="history.back()">
                    <i class="fas fa-arrow-left"></i>
                    Back to Order
                </button>
                <button type="submit" class="btn btn-primary" id="submitBtn">
                    <i class="fas fa-paper-plane"></i>
                    Request Delivery
                </button>
            </div>
        </form>
    </div>

    <!-- Footer -->
    <%- include('./partials/footer') %>

    <script>
        // Get order ID from URL parameters
        const urlParams = new URLSearchParams(window.location.search);
        const orderId = urlParams.get('orderId');

        if (!orderId) {
            window.location.href = '/dashboard';
        }

        // Load order details
        async function loadOrderDetails() {
            try {
                const response = await fetch(`/api/orders/${orderId}`);
                const result = await response.json();

                if (result.success) {
                    displayOrderDetails(result.order, result.items);
                } else {
                    document.getElementById('orderDetails').innerHTML =
                        '<p style="color: #dc3545;">Failed to load order details.</p>';
                }
            } catch (error) {
                console.error('Error loading order details:', error);
                document.getElementById('orderDetails').innerHTML =
                    '<p style="color: #dc3545;">Error loading order details.</p>';
            }
        }

        function displayOrderDetails(order, items) {
            const container = document.getElementById('orderDetails');

            let html = `
                <div class="order-detail-item">
                    <span>Order ID:</span>
                    <span>#${order.id}</span>
                </div>
                <div class="order-detail-item">
                    <span>Order Date:</span>
                    <span>${new Date(order.created_at).toLocaleDateString()}</span>
                </div>
                <div class="order-detail-item">
                    <span>Items:</span>
                    <span>${items.length} item(s)</span>
                </div>
                <div class="order-detail-item">
                    <span>Status:</span>
                    <span style="text-transform: capitalize;">${order.status}</span>
                </div>
                <div class="order-detail-item">
                    <span>Total Amount:</span>
                    <span>TSH ${order.total_amount.toFixed(2)}</span>
                </div>
            `;

            container.innerHTML = html;
        }

        // Delivery method selection
        document.querySelectorAll('.delivery-method').forEach(method => {
            method.addEventListener('click', function() {
                // Remove selected class from all methods
                document.querySelectorAll('.delivery-method').forEach(m => {
                    m.classList.remove('selected');
                });

                // Add selected class to clicked method
                this.classList.add('selected');

                // Update hidden input
                const methodValue = this.dataset.method;
                document.getElementById('deliveryMethod').value = methodValue;
            });
        });

        // Form submission
        document.getElementById('deliveryForm').addEventListener('submit', async function(e) {
            e.preventDefault();

            const submitBtn = document.getElementById('submitBtn');
            const alertDiv = document.getElementById('alertMessage');

            // Clear previous messages
            alertDiv.className = 'alert';
            alertDiv.style.display = 'none';

            // Get form data
            const formData = new FormData(this);

            // Show loading state
            submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Submitting...';
            submitBtn.disabled = true;

            try {
                const response = await fetch(`/api/delivery/request/${orderId}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        deliveryAddress: formData.get('deliveryAddress'),
                        phoneNumber: formData.get('phoneNumber'),
                        deliveryInstructions: formData.get('deliveryInstructions'),
                        deliveryMethod: formData.get('deliveryMethod'),
                        preferredDate: formData.get('preferredDate')
                    })
                });

                const result = await response.json();

                if (result.success) {
                    alertDiv.className = 'alert alert-success';
                    alertDiv.textContent = 'Delivery request submitted successfully! You will be redirected to order tracking.';
                    alertDiv.style.display = 'block';

                    // Redirect to order details after 3 seconds
                    setTimeout(() => {
                        window.location.href = `/orders/${orderId}`;
                    }, 3000);
                } else {
                    alertDiv.className = 'alert alert-error';
                    alertDiv.textContent = result.message || 'Failed to submit delivery request.';
                    alertDiv.style.display = 'block';
                }
            } catch (error) {
                console.error('Error submitting delivery request:', error);
                alertDiv.className = 'alert alert-error';
                alertDiv.textContent = 'Failed to submit delivery request. Please try again.';
                alertDiv.style.display = 'block';
            } finally {
                submitBtn.innerHTML = '<i class="fas fa-paper-plane"></i> Request Delivery';
                submitBtn.disabled = false;
            }
        });

        // Initialize page
        document.addEventListener('DOMContentLoaded', function() {
            loadOrderDetails();
        });
    </script>
</body>
</html>